Programs are made to be run with NodeJS.

version2 is the first tested version, with some parts that doesn't fit in.

version3 contains the following:
* rsa.js which is the final RSA implementation, with a functioning demo.
* benchmark.js which is the benchmarking program, which utilizes methods in RSA implementation